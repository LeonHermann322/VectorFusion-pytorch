program: run_painterly_render.py 
method: bayes
metric:
  name: L_total
  goal: minimize
parameters:
  guidance_scale:
    min: 1
    max: 100
  grad_scale:
    min: 0
    max: 1
command:
- ${env}
- ./run.sh
- ${program}
- ${args}